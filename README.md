# Tea Payroll system
   Turns photos of KTDA tea-leaf receipts into a payroll dashboard.

   - sql/      database schema and queries
   - notebooks/ Python prototyping in Jupyter
   - src/       reusable Python code (parser, bot, USSD)
   - receipts/  receipt photos (not committed to GitHub)
   - powerbi/   the Power BI dashboard file
